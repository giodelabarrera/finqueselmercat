{#{% extends 'base.html.twig' %}

{% block body %}
    <h1>BankAwarded creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_bankawarded_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
#}

{#{% extends 'admin/crud/base_edit.html.twig' %}#}


{% extends 'admin/base.html.twig' %}

{% block title %}
    {#{% if admin.id(object) is not null %}
        {{ "title_edit"|trans({'%name%': admin.toString(object)|truncate(15) }, 'SonataAdminBundle') }}
    {% else %}
        {{ "title_create"|trans({}, 'SonataAdminBundle') }}
    {% endif %}#}
    title edit o title create
{% endblock %}

{% block navbar_title %}
    {{ block('title') }}
{% endblock %}

{%- block actions -%}
    {#{% include 'SonataAdminBundle:CRUD:action_buttons.html.twig' %}#}
{%- endblock -%}

{% block tab_menu %}
    {#{{ knp_menu_render(admin.sidemenu(action), {'currentClass' : 'active', 'template': sonata_admin.adminPool.getTemplate('tab_menu_template')}, 'twig') }}#}
{% endblock %}

{#{% use 'admin/crud/base_edit_form.html.twig' with form as parentForm %}#}
{% import "admin/crud/base_edit_form_macro.html.twig" as form_helper %}

{% block form %}
    {#{{ block('parentForm') }}#}

    form_helper.render_groups(form)
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create" />
    {{ form_end(form) }}
{% endblock %}
