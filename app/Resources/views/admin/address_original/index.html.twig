{% extends 'base.html.twig' %}

{% block body %}
    <h1>Address list</h1>

    <table>
        <thead>
            <tr>
                <th>Cp</th>
                <th>Cityid</th>
                <th>Streettypeid</th>
                <th>Street</th>
                <th>Number</th>
                <th>Floorid</th>
                <th>Stair</th>
                <th>Door</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for address in addresses %}
            <tr>
                <td><a href="{{ path('admin_address_show', { 'id': address.id }) }}">{{ address.cp }}</a></td>
                <td>{{ address.cityId }}</td>
                <td>{{ address.streetTypeId }}</td>
                <td>{{ address.street }}</td>
                <td>{{ address.number }}</td>
                <td>{{ address.floorId }}</td>
                <td>{{ address.stair }}</td>
                <td>{{ address.door }}</td>
                <td>{{ address.id }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_address_show', { 'id': address.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_address_edit', { 'id': address.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_address_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
